stage: sft
description: Server template for Qwen2.5-VL-3B LoRA/SFT.
model_name_or_path: Qwen/Qwen2.5-VL-3B-Instruct
dataset: drivemind_llamafactory_sft
dataset_path: data/processed/drivemind_llamafactory_sft.json
output_dir: outputs/checkpoints/qwen25vl_3b_lora
finetuning_type: lora
lora_rank: 16
lora_alpha: 32
lora_dropout: 0.05
batch_size: 1
gradient_accumulation_steps: 8
learning_rate: 0.0001
num_train_epochs: 1
max_samples: 1000
bf16: true
gradient_checkpointing: true
dry_run_default: true
todo:
  - Adapt to the selected LLaMA-Factory version on the server.
  - Confirm image processor fields and multimodal template.

