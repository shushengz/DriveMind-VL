# DriveMind-VL 目前进展

更新时间：2026-05-27  
对应仓库：`shushengz/DriveMind-VL`，远端默认分支 `initial-mvp`  
当前主结论依据：strict visual-control held-out 评测、DriveLM OOD 诊断、Reward Harness 离线审计

## 1. 一句话结论

DriveMind-VL 已从早期 25 条样本的 `SFT-v0` 链路验证，发展为一套具备 strict visual-control、无泄漏 held-out 评测、answer-only 重评分、SFT/DPO 受控消融、OOD 诊断与 reward 审计的驾驶 VLM 后训练研究框架；当前 Qwen2.5-VL 主模型仍应保留 `SFT-v3-r3`，DPO 与 GRPO 均尚未证明可以稳定替代它。

## 2. 当前研究问题与评测原则

项目的核心问题不是单纯提高驾驶问答 F1，而是判断模型是否真正依赖视觉证据：

- 在真实图像下，模型是否能回答正确；
- 在没有图像、错误图像或空白图像下，模型是否仍会靠语言先验自信作答；
- 后训练是否提升了 normal QA，同时没有让 control 条件也一起变强；
- 模型在 LingoQA 上形成的能力能否泛化到 DriveLM 多相机与空间关系场景。

当前固定使用四路 strict visual-control 输入：

| Setting       | 输入              | 目的                             |
| ------------- | ----------------- | -------------------------------- |
| `normal`      | 真实图像 + 问题   | 检查正常视觉问答能力             |
| `text_only`   | 仅问题            | 检查语言先验作答                 |
| `wrong_image` | 错误图像 + 原问题 | 检查错误视觉干扰与 confound      |
| `blank_image` | 空白图像 + 原问题 | 检查无证据情况下的先验答案与幻觉 |

关键协议已经固定：

- strict visual 模式不注入 perception JSON，不向 control 组泄漏视觉标注；
- 四路 setting 必须按相同 ID 对齐；
- 最终结论只使用无训练重叠的 held-out IDs；
- 对模型输出的主评分使用 `answer_only`，避免 JSON 字段和解释文本污染 F1 与 hallucination 检测；
- held-out 和 DriveLM OOD 样本只用于评测与诊断，禁止回流训练数据。

## 3. 当前最重要的结果

### 3.1 LingoQA held-out 100 最终主表

以下结果来自无训练 ID 重叠的 strict visual-control held-out 评测，指标均为 `answer_only`：

| Model              | Method  |  Normal F1 |    Case Gap | Blank High-F1 | Control High-F1 | Hallucination | Normal Refusal |
| ------------------ | ------- | ---------: | ----------: | ------------: | --------------: | ------------: | -------------: |
| Base Qwen2.5-VL-3B | Base    |     0.2230 |     -0.0548 |        0.2400 |          0.2333 |        0.0433 |         0.0000 |
| DPO-v7             | DPO     |     0.2727 |     -0.0100 |        0.2900 |          0.2767 |        0.0533 |         0.0000 |
| SFT-v3-r2          | SFT     |     0.2434 |     -0.0693 |        0.2500 |          0.2600 |        0.0567 |         0.0000 |
| **SFT-v3-r3**      | **SFT** | **0.3278** | **-0.0011** |        0.2900 |          0.3267 |        0.1000 |         0.0000 |
| DPO-v8 rule-based  | DPO     |     0.3308 |     -0.0109 |        0.3800 |          0.3667 |        0.0900 |         0.0000 |
| DPO-v8.1 step-25   | DPO     |     0.3257 |     -0.0068 |        0.2900 |          0.3367 |        0.0933 |         0.0000 |
| DPO-v8.1 step-50   | DPO     |     0.3168 |     -0.0291 |        0.3800 |          0.3633 |        0.0867 |         0.0000 |
| DPO-v8.2 step-25   | DPO     |     0.3211 |     -0.0127 |        0.2900 |          0.3367 |        0.0967 |         0.0000 |

结论：

- `SFT-v3-r3` 是当前综合最稳的 Qwen2.5-VL checkpoint：Normal F1 高，Case Gap 最接近 0，且没有 normal refusal。
- `DPO-v8` 虽获得略高的 Normal F1，但 control high-F1 和 Case Gap 明显更差，不能作为更好的视觉依赖模型。
- `DPO-v8.1` 与 `DPO-v8.2` 能轻微减少部分显式 hallucination，但没有稳定改善视觉依赖，训练越久还会出现 over-calibration。
- 当前最佳 checkpoint 保留为：

```text
checkpoints/qwen25vl_lora_sft_v3_r3_lingo_smoke/
```

### 3.2 DriveLM OOD 结论

DriveLM 用于考察多相机、object token 与空间关系泛化。现有 100-case OOD 诊断显示：

| Model              | Normal F1 | Case Gap | Control High-F1 | Blank High-F1 | 结论                              |
| ------------------ | --------: | -------: | --------------: | ------------: | --------------------------------- |
| Base Qwen2.5-VL-3B |    0.1566 |  -0.1309 |          0.2033 |             - | 基线较弱，但 control 依赖较低     |
| SFT-v3-r3          |    0.1888 |  -0.1387 |          0.2767 |        0.3000 | normal 风格迁移，但视觉依赖未泛化 |

离线失败归因显示：

- `camera_specific_failure` 覆盖 90/100 cases；
- `spatial_relation` 是样本量足够情况下的突出瓶颈，`case_gap=-0.3234`、`control_high_f1=0.5614`；
- wrong-image confound 共 90 条，并与 object-token / spatial failure 大量交叉；
- Base 到 r3 的 fix/break 为 14/23，说明 r3 在 DriveLM 上损伤样本多于修复样本。

因此，r3 在 LingoQA 上成立的优势不能包装成跨数据集 visual grounding 成功。DriveLM 当前是重要 limitation 与下一轮 reward 设计依据，而不是训练集来源。

## 4. 阶段演进记录

### 4.1 早期基线：从普通问答到严格视觉控制

项目早期通过 prompt/frame ablation 得到较好的 Base 策略：

```text
prompt_variant = spatial
frame_strategy = first_middle_last
max_images = 3
```

该 Base 方案在早期 LingoQA 100 上的结果为：

| Setting        |     F1 |
| -------------- | -----: |
| normal 3-frame | 0.2912 |
| text-only      | 0.1448 |
| wrong 3-frame  | 0.2095 |
| blank 3-frame  | 0.1349 |

当时 setting-level gap 为 `+0.0817`，per-case conservative gap 为 `+0.0212`。这一结果说明：仅报告 normal F1 不足以判断模型是否使用视觉，必须同步加入 control conditions 和 case-level gap。

### 4.2 SFT-v0：25 条样本只是链路 smoke，不是当前方案

`SFT-v0` 使用了 **25 条 reviewed `keep` 样本**，其定位是最小 LoRA 训练与严格评测链路验证。结果为：

| Model               | Normal F1 | Case Gap |
| ------------------- | --------: | -------: |
| Base early strategy |    0.2912 |  +0.0212 |
| SFT-v0, 25 samples  |    0.2470 |  -0.0412 |

这一实验已经被明确判定为失败的模型改进方案：训练数据过小且过度偏向 anti-hallucination / wrong-image confound，导致正常视觉问答能力下降。仓库中保留该报告是为了记录实验历史，不代表当前仍在使用 SFT-v0。

### 4.3 CPU-only visual-control 基础设施

随后工程被重构为可复现的 Visual-Control 后训练框架，完成了：

- LingoQA / DriveLM strict visual-control 输入构造；
- SFT-v3 与 Preference-v8 数据管线；
- raw prediction 汇总、F1/EM、gap、hallucination/refusal 指标；
- offline reward harness 与 case gallery；
- CPU-only dry-run 与单元测试入口。

这一阶段的重要价值是把训练前置风险转为可检测的工程约束：输入泄漏、字段混乱、setting 未对齐和 default GPU 路径都不再被默许。

### 4.4 Stage 2：SFT-v3 smoke 暴露数据比例错误

Stage 2 对 Base、SFT-v2、DPO-v7 与第一版 SFT-v3 smoke 进行了 LingoQA strict eval。50-case 结果包括：

| Model        | Normal F1 | Case Gap | Control Hallucination |
| ------------ | --------: | -------: | --------------------: |
| Base         |    0.2074 |  -0.0368 |                0.3467 |
| SFT-v2       |    0.2815 |        - |                     - |
| DPO-v7       |    0.2841 |        - |                  较高 |
| SFT-v3 smoke |    0.2008 |  -0.0109 |                0.2600 |

SFT-v3 smoke 降低了 hallucination，并改善了 case gap，但 Normal F1 低于 Base。审计后发现数据组成失衡：

```text
normal_visual_qa:       229
blank_refusal:          228
wrong_image_caution:    228
text_only_caution:      228
spatial_hard_negative:   87
```

normal 仅约 23%，远低于原定的 55% 到 60%。这一失败直接促成了 ratio guard、normal anchor 强化和后续 r2/r3 数据修复。

### 4.5 Stage 3：SFT-v3-r2 修比例，但输出格式污染评测

SFT-v3-r2 提高 normal anchor、限制 control/refusal 比例，并加入数据审计。然而 r2 输出为：

```json
{"answer": "...", "reason": "..."}
```

这引出两个新问题：

- raw full-prediction F1 会被 JSON 字段和 `reason` 文本稀释；
- `reason` 中的 `image`、`visual`、`visible`、`scene` 等词会误触发 hallucination 规则。

因此，r2 的 raw 指标低估了 normal QA，且其控制行为判断不够干净。项目由此将主评测口径切换为 `answer_only`，并为 r3 取消 `reason` 字段。

### 4.6 Stage 4：SFT-v3-r3 answer-only calibration

r3 的关键改变是：

- 从较稳的 `SFT-v2` adapter 继续轻量校准，而不是重新从 Base 学习；
- 输出格式变为仅 `{"answer": "..."}`；
- normal-like 数据占比提高到 `86.25%`，control 比例降为 `13.75%`；
- r3 数据审计通过：JSON valid rate `1.0`、normal refusal rate `0.0`、control forbidden visual terms rate `0.0`、无 reason 与 metadata 泄漏。

Stage 4 原始结果非常亮眼，但随后被发现存在评测泄漏。

### 4.7 Stage 4.5：发现 100% train/eval leakage，并重跑 held-out

泄漏审计发现 Stage 4 的原评测集合与 r3 训练 ID：

```text
overlap_rate_eval = 100%
```

因此，Stage 4 原始高分不能作为泛化结论。项目立即固定无训练重叠的 held-out 100 IDs，并对各模型重新评测。新的 held-out 结果表明：

- r3 的 `normal_f1=0.3278`，高于 Base、SFT-v2、DPO-v7 与 r2；
- r3 的 `case_gap=-0.0011`，最接近 0；
- 但 r3 的 `control_hallucination=0.1000` 偏高。

这一步是项目可信度最重要的修正之一：后续所有模型选择均基于 held-out，而非被污染的 Stage 4 分数。

### 4.8 Stage 5-6：rule-based Preference-v8 与 DPO-v8

Preference-v8 使用规则构造 pairs，并严格排除 held-out IDs：

- total pairs = `500`；
- normal pairs = `55%`；
- control pairs = `37%`；
- spatial pairs = `8%`；
- held-out leakage = `0`；
- `uses_model_predictions=false`。

DPO-v8 从 r3 初始化，以 frozen r3 为 reference，训练 50 steps。结果：

| Model  | Normal F1 | Case Gap | Hallucination |
| ------ | --------: | -------: | ------------: |
| r3     |    0.3278 |  -0.0011 |        0.1000 |
| DPO-v8 |    0.3308 |  -0.0109 |        0.0900 |

DPO-v8 保住 normal QA，轻微减少显式 hallucination，但 Case Gap 恶化。问题在于规则 pairs 没有准确击中 r3 的真实 text-prior、blank prior 与 wrong-image confound。

### 4.9 Stage 7-8：model-mined Preference-v8.1 与 DPO-v8.1

项目在排除 held-out 的 mining pool 上运行 r3 prediction，并构造真实 rejected answer：

- hard negatives = `1078`；
- Preference-v8.1 pairs = `500`；
- `uses_model_predictions=true`；
- `rejected_from_actual_prediction_rate=1.0`；
- held-out leakage = `0`；
- normal/control/spatial = `26% / 67% / 7%`。

DPO-v8.1 同时评估 step-25 与 step-50：

| Model            | Normal F1 | Case Gap | Hallucination |
| ---------------- | --------: | -------: | ------------: |
| r3               |    0.3278 |  -0.0011 |        0.1000 |
| DPO-v8.1 step-25 |    0.3257 |  -0.0068 |        0.0933 |
| DPO-v8.1 step-50 |    0.3168 |  -0.0291 |        0.0867 |

结论是典型的 over-calibration：步数越多，显式 hallucination 越低，但 normal F1 和 case gap 越差。显式幻觉下降不能替代视觉依赖改善。

### 4.10 Stage 8.5-10：从 hallucination 转向 case-gap-aware DPO-v8.2

Stage 8.5 的离线归因找到了 DPO-v8.1 的核心失败模式：

- case-gap 恶化样本共 `26` 个；
- 其中 blank-image 回归 `14` 个、normal F1 下降 `6` 个、text-only 回归 `6` 个；
- blank-image F1 从 r3 的 `0.2022` 上升到 step-50 的 `0.2412`；
- high-F1 control rate 从 r3 的 `0.3267` 上升到 step-50 的 `0.3633`。

这说明模型可能不再说出触发 hallucination 规则的视觉描述，却转而生成 `Yes`、`No`、数字或动作等短直接答案；这些答案仍会在 control 条件下与 gold 高度重合并损害 Case Gap。

Preference-v8.2 因此显式引入：

- `control_direct_answer_vs_caution`；
- `blank_high_f1_vs_caution`；
- `text_only_gold_overlap_vs_caution`；
- `wrong_image_gold_overlap_vs_caution`；
- 更高比例的 normal anchor。

DPO-v8.2 从 r3 与 frozen r3 reference 出发，只训练 25 steps。实际结果：

| Metric          |      r3 | DPO-v8.2 step-25 |
| --------------- | ------: | ---------------: |
| Normal F1       |  0.3278 |           0.3211 |
| Case Gap        | -0.0011 |          -0.0127 |
| Blank High-F1   |  0.2900 |           0.2900 |
| Control High-F1 |  0.3267 |           0.3367 |
| Hallucination   |  0.1000 |           0.0967 |
| Normal Refusal  |  0.0000 |           0.0000 |

训练本身稳定完成：`final_loss=0.682665`、`final_reward_margin=0.021126`，无 NaN/OOM。但训练目标未成功改善 Case Gap，DPO-v8.2 被判定失败；不追加 DPO steps。

### 4.11 Stage 11-15：收束结论、OOD 与 reward 路线准备

Stage 11 将当前实验收束为正式结论：SFT-v3-r3 是现有 Qwen2.5-VL 主模型，DPO 分支是重要消融而非替代方案。

Stage 12-13 将评测扩展到：

- 尝试建立更大的 LingoQA clean held-out，但排除现有训练/mining 后可用干净 ID 仍只有 `100` 条；
- 在 DriveLM 上完成 OOD strict visual-control 诊断，证明 normal style transfer 并不等于跨域 visual grounding。

Stage 14 系列进行离线 Reward Harness 校准：

| Reward 版本 | 结果                                                         | 决策                           |
| ----------- | ------------------------------------------------------------ | ------------------------------ |
| v2          | 加入 camera/spatial/object 惩罚，但排名对权重敏感            | 不训练 GRPO-lite               |
| v2.1        | 离线 gate 改善，LingoQA 排名回到 `r3 > DPO-v8.1 > DPO-v8.2 > DPO-v8`，pairwise accuracy `0.9752` | 仅可考虑人工复核后的最小 smoke |
| v2.2        | 人工复核后加入 invariant/template/control-copy 等惩罚，但排名又变为 `DPO-v8.1 > DPO-v8.2 > r3 > DPO-v8`，pairwise accuracy `0.9277` | **仍不训练 GRPO-lite**         |

Stage 15 只准备 Qwen3-VL 开源训练栈，不改变 Qwen2.5 的实验结论：

- 主训练栈候选为 `ms-swift`；
- 已准备 Qwen3-VL 数据导出与 base strict eval 入口；
- 如后续允许 GPU，应先评估 Qwen3-VL-4B Base 的 LingoQA held-out 100 与 DriveLM OOD 100；
- 不允许把“换更强模型”替代为 visual grounding 的证据。

## 5. 失败经验与已经固定下来的工程原则

| 失败或风险              | 观察到的现象                                      | 根因判断                                     | 当前固定原则                                                 |
| ----------------------- | ------------------------------------------------- | -------------------------------------------- | ------------------------------------------------------------ |
| SFT-v0 小样本训练       | 25 条 SFT 后 normal 与 gap 都下降                 | 样本太少，且过度偏向 control/抗幻觉          | 小数据仅用于 smoke，不可包装成模型提升                       |
| 初版 SFT-v3             | hallucination 降，但 Normal F1 下降               | normal 仅约 23%，拒答/控制样本过多           | 所有 SFT 数据必须有 ratio guard 与 normal anchor             |
| SFT-v3-r2 raw 评分      | raw F1 与 hallucination 口径失真                  | JSON/reason 混入答案和检测词                 | 最终指标统一用 answer-only；输出尽量 answer-only             |
| Stage 4 原始高分        | r3 看似极强                                       | eval IDs 与训练 IDs 100% 重叠                | 所有模型选择必须先做 normalized-ID leakage audit             |
| DPO-v8 rule-based       | hallucination 略降，但 gap 变差                   | pairs 不是来自模型真实失败                   | Preference 优先 model-mined，规则构造只做 baseline           |
| DPO-v8.1 继续训练       | step-50 比 step-25 更低 hallucination，却更差 gap | control 强度过高，出现 over-calibration      | 必须评估中间 checkpoint，不因单一指标下降而延长训练          |
| 只看 hallucination      | 幻觉变少但 case gap 更差                          | 短 direct/prior answer 未被旧规则识别        | 同时报告 high-F1 control、direct answer、blank high-F1 与 case gap |
| DPO-v8.2 case-gap-aware | 方向合理但仍未胜过 r3                             | 现有偏好目标尚不足以稳定修复 control overlap | 当前保留 r3，不盲目继续 DPO                                  |
| DriveLM OOD             | r3 normal 上升但 gap 恶化                         | camera/object/spatial 泛化失败               | OOD 作为限制与 reward 设计证据，不能掩饰为成功               |
| Reward v2.x             | 不同 patch 会改变模型排名                         | reward 与人工判断尚未充分一致                | GRPO 前必须先通过人工复核、敏感性分析与 sanity gate          |
| 评测集规模              | leakage-free LingoQA 仅 100 IDs                   | 训练/mining 消耗了可用同源样本               | 下一阶段优先扩大独立、不可回流训练的评测集                   |

这些失败不是无效开销，它们共同把项目从“训练一次看分数”推进到“能解释为什么分数变化、知道哪些结论不能说”的阶段。

## 6. 已完成的能力与主要产物

### 6.1 数据与协议

- strict visual-control formatter：LingoQA 多帧、DriveLM 多相机及 question-aware camera selection；
- SFT-v3-r2 / r3 构造与审计，含 ratio guard 与 forbidden-term guard；
- Preference-v8 / v8.1 / v8.2 构造、held-out exclusion 与审计；
- mining pool 与 model-mined hard negative 工具；
- held-out ID 构建和 train/eval overlap 审计。

### 6.2 评测与诊断

- raw predictions 保存与 visual-control 指标汇总；
- answer-only parser、rescore 和共同 ID 主表；
- control behavior metrics、case-gap regression、fix/break case analysis；
- LingoQA held-out 与 DriveLM OOD case gallery；
- final report 与阶段诊断报告。

### 6.3 训练与运行边界

- Qwen2.5-VL SFT-v3-r3 lightweight calibration smoke；
- DPO-v8、v8.1、v8.2 受控 smoke，均从 r3 / frozen r3 reference 出发；
- readiness gate、dry-run 总控脚本、失败保护；
- Reward Harness v2/v2.1/v2.2 离线审计；
- Qwen3-VL 开源训练栈与数据导出准备。

## 7. 当前明确决策

### 7.1 保留什么

- 当前 Qwen2.5-VL 主模型：`SFT-v3-r3`。
- 当前可信评测基准：LingoQA leakage-free held-out 100 strict visual-control。
- 当前 OOD 限制证据：DriveLM 100 strict visual-control。
- DPO-v8/v8.1/v8.2：作为偏好目标失败模式和消融证据保留。

### 7.2 不做什么

- 不再把 `SFT-v0` 当作当前训练状态或有效主模型；
- 不使用 Stage 4 泄漏评测结果作为泛化结论；
- 不增加 DPO-v8.x 训练步数来“赌”指标改善；
- 不将 held-out 或 DriveLM OOD 数据回流到 SFT/DPO/GRPO 训练；
- 当前不训练 GRPO-lite；
- 不因 Qwen3-VL 栈准备完成就宣称模型性能升级。

## 8. 下一步规划

按研究可信度优先级，下一步建议如下：

1. **扩大独立评测证据。** 获取或构建与现有 SFT/mining 数据彻底隔离的新 LingoQA-like strict visual-control 集；现有 clean held-out 仅 100 条，不足以支撑更细模型选择。
2. **先跑 Qwen3-VL Base 对照，而不是直接训练。** 在同一 LingoQA held-out 100 与 DriveLM OOD 100 上评估 Qwen3-VL-4B Base，判断架构升级本身能否改善 visual dependency。
3. **继续校准 Reward Harness，而非直接 GRPO。** 基于人工复核修正 v2.2 中仍会错排模型的 penalty / reward 逻辑，特别是 camera/object/spatial 与 control-copy 行为。
4. **如继续 DPO，先设计 v8.3 而非追加步数。** v8.3 必须围绕 blank/control high-F1 与 direct-prior 答案建立更稳的 pairs，并加大 normal anchor；仍须从 r3 出发，用独立评测决定是否保留。
5. **GRPO-lite 仍是条件性路线。** 只有 reward 排序稳定、人工复核通过、训练池与评测池隔离完整，并且在扩大的独立评测上仍存在可优化的 control failure 时，才允许做最小 GPU smoke。

## 9. 可核对的仓库材料

当前结论可通过以下文件复核：

| 文件                                                       | 用途                                                   |
| ---------------------------------------------------------- | ------------------------------------------------------ |
| `README_CN.md`                                             | Stage 2 到 Stage 15 的总体记录与当前决策               |
| `docs/lingoqa_stage_artifact_index.md`                     | 早期 LingoQA 评测、frame/prompt 策略及 SFT-v0 前置背景 |
| `docs/lingoqa_sft_v0_adapter_eval_report.md`               | 25 条 SFT-v0 smoke 失败结论                            |
| `configs/sft_v3_r3_lingo_smoke.yaml`                       | 当前主模型 r3 的训练配置                               |
| `configs/dpo_v8_2_lingo_smoke.yaml`                        | case-gap-aware DPO 的受控配置                          |
| `scripts/run_stage10_dpo_v8_2_gpu_smoke.sh`                | DPO-v8.2 25-step smoke 的严格执行边界                  |
| `scripts/run_stage11_final_report.sh`                      | 最终结果汇总入口                                       |
| `scripts/run_stage13_5_drivelm_ood_failure_attribution.sh` | DriveLM OOD 失败归因入口                               |
| `scripts/run_stage14_6_reward_v2_2_cpu.sh`                 | 当前 reward patch 与人工复核整合入口                   |
| `scripts/run_stage15_qwen3_vl_stack_prepare_cpu.sh`        | Qwen3-VL 栈准备入口                                    |

## 10. 目前最应记住的判断

1. `SFT-v0` 确实只用了 25 条小样本，但它是已经被淘汰的早期 smoke baseline。
2. `SFT-v3-r3` 是当前主模型，因为它在无泄漏 held-out 上取得了最佳综合 normal QA 与 case gap 表现。
3. DPO 三轮实验没有证明能稳定改善 visual dependency；减少 hallucination 不等于改善 case gap。
4. DriveLM 表明跨域视觉依赖仍是明显短板，不能只看 LingoQA 的 normal F1。
5. Reward Harness 仍在校准期，当前不具备直接启动 GRPO-lite 的充分证据。
6. 下一阶段最有价值的工作是扩大独立评测、进行 Qwen3-VL Base 对照和继续修正可审计 reward，而不是盲目增加训练规模。
